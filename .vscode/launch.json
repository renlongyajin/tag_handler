{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run mytest.py",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/mytest.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
