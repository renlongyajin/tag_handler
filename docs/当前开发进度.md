2025-11-03 重构 tag_viewer：拆分成 tagger 包（config、translation、widgets、fileops、commands、main_window、app、utils），确保核心文件不超过 300 行并优化逻辑。
2025-11-03 修复 ImageViewer 加载 QPixmap 时的 QRect/QRectF 类型不匹配，保证切换目录时图片正常显示。
2025-11-03 标签界面改为英文+中文配对展示，默认分栏比例 1:2，并引入多级翻译管线。
2025-11-03 新增当前文件名显示、左右键非循环导航、Ctrl+Z/Ctrl+Shift+Z 撤销体系，以及双列标签布局。
2025-11-03 优化前端体验：窗口默认全屏，图片自适应显示，标签行固定高度并使用护眼配色。
2025-11-03 加粗标签字体与按钮文本，提升可读性。
2025-11-03 增加批量删除标签功能，可一键移除指定标签并输出处理结果。
2025-11-03 支持中文标签输入：自动翻译生成中英文对应项。
2025-11-03 标签面板新增复制/粘贴按钮，可跨文件快速复用标签。
2025-11-03 文件锁定机制：允许标记完成、禁用编辑、按钮与状态栏显示 🔓/🔒 图标提示，无需弹窗确认。
2025-11-03 菜单新增一键锁定/解锁全部标签与锁定统计功能。
2025-11-03 菜单新增导出标签功能，支持将全部文件标签以 JSON 格式保存。
2025-11-03 导出功能新增导出已锁定标签按钮，生成 JSON 映射。
2025-11-04 新增批量添加 / 批量替换标签对话框，可一次录入多个标签并可选择影响已锁定文件。
2025-11-04 新增范围锁定功能，支持通过起始和结束序号批量锁定文件。
2025-11-03 新增删除并重排功能，支持删除指定序号及所有同名文件并顺序前移。
